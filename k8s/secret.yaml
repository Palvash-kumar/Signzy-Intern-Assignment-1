# Replace placeholder values before applying.
# In production, use: kubectl create secret generic api-orchestrator-secrets \
#   --from-literal=JWT_SECRET=<your-secret> \
#   --from-literal=API_KEY=<your-key> \
#   --from-literal=GROQ_API_KEY=<your-key>
apiVersion: v1
kind: Secret
metadata:
  name: api-orchestrator-secrets
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  API_KEY: "CHANGE_ME_IN_PRODUCTION"
  GROQ_API_KEY: "CHANGE_ME_IN_PRODUCTION" 
  LLM_PROVIDER: "groq"

